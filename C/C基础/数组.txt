数组是相同数据类型变量的有序集合
    数组是一个整体需要一个合法命名；
    数组中变量没有独立命名；
    数组中变量的数量固定；
数组定义
    - 语法：type name[size]；
注意：
    -数组名只能当作左值
    -只能使用整型做下标

多维数组
    int a[2][3] ={0}
    int a[][2] ={{1,2},{2,5}}
    int a[][2] = {1,4,6}